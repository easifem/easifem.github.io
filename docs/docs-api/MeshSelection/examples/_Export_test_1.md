This example shows how to USE `MeshSelection` WITH `meshSelectionByID` option,
and export it to `HDF5File_` format.

import CodeBlock from '@theme/CodeBlock';

import CodeSnippet from '!!raw-loader!./_Export_test_1.F90';

<CodeBlock language="fortran">{CodeSnippet}</CodeBlock>

