# SetOrder

```fortran
SUBROUTINE obj_SetOrder(obj, order)
```

Changes the order of an already initialized `AbstractOneDimFE_` object and reallocates internal data as needed.

#### Arguments

- `obj` - The AbstractOneDimFE_ object
- `order` - The new element order
