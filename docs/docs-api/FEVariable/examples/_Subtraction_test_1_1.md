- This example demonstrates the Subtraction of two Scalar FEVariables.
- The variables `obj1`, `obj2` can be `constant`, `space`, `time`, or `spaceTime`.
- The `varType` of ans depends on the `varType` of `obj1` and `obj2`.

import CodeBlock from '@theme/CodeBlock';

import CodeSnippet from '!!raw-loader!./_Subtraction_test_1.F90';

<CodeBlock language="fortran">{CodeSnippet}</CodeBlock>
